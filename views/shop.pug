extends layouts/main-layout.pug

block styles
        link(rel="stylesheet", href="/css/main.css")
block content
        if prods.length > 0
                h1 My products
                    each product in prods
                        p #{product.title}
        else
            h1 no products                                 